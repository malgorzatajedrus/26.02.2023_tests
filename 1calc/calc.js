const calculator = {
    sum: function (a,b) {
        return a + b;
    },
    diff: (a,b) => a - b,
    
    // function (a,b) {
    //     return a - b;
    // },
    multi: (a,b) => a * b,
};

module.exports = calculator;

